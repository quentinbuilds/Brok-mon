# Godot
.godot/
*.translation
export/
*.tmp

# Summer (keep .summer/ project memory, drop caches)
.summer/cache/

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Summer Engine local-only state (SummerGit, caches, locks)
.summer/local/

# Export artifacts (ship-to-unoq writes build/game-linux-arm64.zip)
build/

# Godot editor backups
*.bak

# Raw downloads kept beside the scene for convenience. The game uses the converted .wav files
# in assets/audio/, which are committed; these originals are not needed by anyone else.
audio/*.mp3
audio/*.mp3.import
